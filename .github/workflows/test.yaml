name: Test Host List Parsing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  regression-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - host: ssw-giga-04
            suite: atom-plus-4.robot
            num_device: 4
          - host: ssw-giga-05
            suite: atom-plus-8.robot
            num_device: 8
          - host: ssw-giga-06
            suite: atom-plus-16.robot
            num_device: 16
    outputs:
      host-list: ${{ steps.collect-hosts.outputs.host-list }}
    steps:
    - name: Set host information
      id: collect-hosts
      run: |
        echo "Matrix host: ${{ matrix.host }}"
        echo "host=${{ matrix.host }}" >> $GITHUB_OUTPUT
        echo "host-list=${{ matrix.host }}" >> $GITHUB_OUTPUT
        
    - name: Save host to shared file
      run: |
        echo "Saving host ${{ matrix.host }} to shared file"
        echo "${{ matrix.host }}" >> /tmp/all-hosts.txt

  merge-result:
    needs: regression-test
    runs-on: ubuntu-latest
    if: always()
    steps:
    - name: Test host list parsing logic
      run: |
        echo "=== Testing Host List Parsing Logic ==="
        
        echo "=== Method 1: From shared file ==="
        if [ -f "/tmp/all-hosts.txt" ]; then
          echo "Reading hosts from shared file..."
          cat /tmp/all-hosts.txt
          echo ""
          
          HOST_LIST=$(cat /tmp/all-hosts.txt | sort -u | tr '\n' ' ' | sed 's/ $//')
          echo "✅ Parsed from file: $HOST_LIST"
        else
          echo "❌ Shared file not found"
          echo "Trying job outputs as fallback..."
          
          # Fallback: job outputs 사용
          HOST_LIST_OUTPUT='${{ toJson(needs.regression-test.outputs.host-list) }}'
          echo "Raw output: $HOST_LIST_OUTPUT"
          
          if [ "$HOST_LIST_OUTPUT" != "null" ] && [ "$HOST_LIST_OUTPUT" != "[]" ] && [ -n "$HOST_LIST_OUTPUT" ]; then
            HOST_LIST="$HOST_LIST_OUTPUT"
            echo "✅ Parsed from outputs: $HOST_LIST"
          else
            echo "❌ No hosts found"
            exit 1
          fi
        fi
        
        echo ""
        echo "🎯 Final host list: $HOST_LIST"
        echo "🎯 Host count: $(echo $HOST_LIST | wc -w)"
        
        # 실제 사용 예시
        echo ""
        echo "=== Usage Example ==="
        for host in $HOST_LIST; do
          echo "  Processing host: $host"
        done
        

        

